<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passio">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".order.GioHang"></activity>

        <activity android:name=".account.DatHang" />
        <activity android:name=".home.ChiTiet" />
        <activity android:name=".member.MemberCard" />
        <activity android:name=".noti.Notifications" />
        <activity android:name=".account.Account" />
        <activity android:name=".order.Order" />
        <activity android:name=".qua.Qua" />
        <activity android:name=".home.Home" />
        <activity android:name=".LoginProfile" />
        <activity android:name=".LoginSdtChild" />
        <activity android:name=".LoginSdt" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>